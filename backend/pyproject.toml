[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["Cj-bc <cj-bc@y-modify.org>"]

[tool.poetry.dependencies]
python = "^3.5"
flask = "^1.0"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
